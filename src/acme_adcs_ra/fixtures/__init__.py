"""Pre-generated, non-sensitive fixture certificates for dev/CI.

These PEMs are NOT private keys and do NOT chain to any real CA. They exist so
FakeEnrollmentLeg can return realistic-looking certificate data without ever
invoking a signing primitive.
"""
